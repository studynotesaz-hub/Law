export interface NavItem {
  label: string;
  href: string;
}

export interface CaseStudy {
  id: number;
  title: string;
  category: string;
  year: string;
  description: string;
}

export interface PracticeArea {
  title: string;
  description: string;
  icon: string;
}
